{
  "scripts": {
    "start": "concurrently \"npm run client\" \"npm run server\"",
    "client": "cd client && npm install react-scripts && npm start",
    "server": "cd server && npm install express && npm start"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "react-router-dom": "^6.23.1"
  }
}
